# Shared skills

Before invoking a skill, fetch origin/main and compare the worktree and commit. Fast-forward only when clean; preserve local edits and reconcile divergence. Re-read the selected SKILL.md after updates.
Keep customer profiles, credentials, API authorization and learning evidence in the user's own private workspace. This public repository grants no authorization to call paid services or upload media.
Preserve draft and external-dependency statuses. Only publish changes when the current user has authorized their target repository and action.
